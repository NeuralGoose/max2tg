[pytest]
# Register custom markers so the default run can exclude live tests with
# `-m "not integration"` without raising PytestUnknownMarkWarning.
markers =
    integration: live tests that hit real MAX/Telegram; excluded by default
    slow: interactive or long-running tests (sms/qr login)
